package model;

import java.util.List;

public class PetMatcher {

    public static Pet findBestMatch(UserProfile user, List<Pet> pets) {
        // TODO: implement match logic
        // This should include:
        // - Matching pet type
        // - Matching pet breed
        // - Matching energy level, allergies, budget, time, etc.

        // Example scoring idea:
        // if (user.getPreferredBreeds().contains(pet.getBreed())) score += 5;

        return null;
    }
}
